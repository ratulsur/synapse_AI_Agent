"""Node: Scope & Plan.

Produces the ReportPlan (audience, length, tone, ordered section specs) from the
query and analyst persona. Delegates to ``agents.planner``. This node is the
revise target of the Human-in-the-loop loop. Emits ``plan`` into state.

Owner: backend-developer (planning prompt: agent-prompt-engineer)
"""

# TODO(backend-developer): def scope_plan(state) -> dict
