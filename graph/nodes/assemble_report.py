"""Node: Assemble Report.

Pure function node: joins the grounded ``sections`` in plan order into a single
report string (headings, citations, ordering). No LLM call. Emits ``report``.

Owner: backend-developer
"""

# TODO(backend-developer): def assemble_report(state) -> dict
