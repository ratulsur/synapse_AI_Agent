# Eval datasets

Owner: test-eval-agent

Curated query sets (one per domain: Techno/Education/Travel/Art/Mgmt + GENERIC)
with expected routing labels and reference/grounding annotations.

TODO(test-eval-agent): add dataset files (jsonl) + a schema doc.
