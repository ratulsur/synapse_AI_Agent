"""MCP (Model Context Protocol) tool client.

Connects to configured MCP servers and surfaces their tools to the ReAct agent
alongside the native tools. Server endpoints configured via configuration.yaml
(read through utils.config_loader.load_config).

Owner: backend-developer
"""

# TODO(backend-developer): implement MCP client + tool adapter.
