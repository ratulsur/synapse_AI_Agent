"""Web search tool (GENERIC fallback + broad coverage).

Wraps a web search provider as a LangChain tool returning raw hits (title, url,
snippet) for the normalize step.

Owner: backend-developer
"""

# TODO(backend-developer): implement web_search tool.
