"""Route handlers and request/response DTOs for the API.

DTOs should re-use schemas/ models (Source, ReportPlan, Section) so the frontend
contract stays in lockstep with the graph state.

Owner: backend-developer (DTO shape co-owned with frontend-ui-developer)
"""

# TODO(backend-developer): implement route handlers + DTOs.
